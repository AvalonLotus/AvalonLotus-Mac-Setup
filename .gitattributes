# Keep line endings consistent so shell/python scripts never break in
# Docker/Linux from a CRLF checkout. Store LF in the repo; force LF for files
# executed as scripts.
* text=auto

*.sh      text eol=lf
*.bash    text eol=lf
*.py      text eol=lf
*.command text eol=lf
